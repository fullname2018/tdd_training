/* global expect, it, describe */

import actions from '.';
import types from '../constants/';

describe("", () => {
  it("", () => {

  })
})

/*
describe('Actions', () => {
  const todoText = 'A todo';

  it('Должно создаться событие для добавления задания', () => {
    const expectedAction = {
      type: types.SUBMIT_TODO,
      id: 1,
      text: todoText,
    };

    expect(actions.submitTodo(todoText)).toEqual(expectedAction);
  });

  it('Должно создаться событие для удаления задания', () => {
    const expectedAction = {
      type: types.DELETE_TODO,
      id: 1,
    };

    expect(actions.deleteTodo(1)).toEqual(expectedAction);
  });
});
*/